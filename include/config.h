#ifndef CONFIG_H
#define CONFIG_H

// ======== ОБЩИЕ НАСТРОЙКИ ========

// Имя устройства (используется для SSID в режиме AP)
#define DEVICE_NAME "M5WiFiDebugger"

// Пароль по умолчанию для режима AP
#define DEFAULT_AP_PASSWORD "12345678"

// Канал WiFi по умолчанию для режима AP
#define DEFAULT_AP_CHANNEL 1

// ======== НАСТРОЙКИ ПИТАНИЯ ========

// Минимальное напряжение батареи для предупреждения
#define LOW_BATTERY_VOLTAGE 3.3

// Критическое напряжение батареи для автоматического выключения
#define CRITICAL_BATTERY_VOLTAGE 3.2

// Таймаут автоматического выключения при критическом заряде (миллисекунды)
#define CRITICAL_BATTERY_TIMEOUT 300000  // 5 минут

// Таймаут автоматического перехода в режим сна при бездействии (миллисекунды)
#define INACTIVITY_SLEEP_TIMEOUT 1800000  // 30 минут

// ======== НАСТРОЙКИ KVM ========

// Максимальное количество пинов в KVM
#define MAX_KVM_PINS 10

// Частоты звуковых сигналов для режима BUZZ
#define KVM_BUZZ_HIGH_TONE 2000
#define KVM_BUZZ_LOW_TONE 1000
#define KVM_BUZZ_DURATION 100

// ======== НАСТРОЙКИ HONEYPOT ========

// Максимальное количество соединений в логе Honeypot
#define MAX_HONEYPOT_CONNECTIONS 10

// ======== НАСТРОЙКИ IR-КОНТРОЛЛЕРА ========

// Пин для ИК-передатчика
#define IR_TX_PIN 9

// Максимальное количество сохраненных ИК-команд
#define MAX_IR_COMMANDS 20

// ======== НАСТРОЙКИ ОТЛАДКИ ========

// Включение отладочных сообщений в Serial
#define DEBUG_ENABLED true

// Скорость Serial
#define SERIAL_BAUD_RATE 115200

// ======== НАСТРОЙКИ ФАЙЛОВОЙ СИСТЕМЫ ========

// Форматировать файловую систему при первом запуске
#define FORMAT_FILESYSTEM_IF_FAILED true

// ======== НАСТРОЙКИ МЕНЮ ========

// Количество строк на экране для отображения меню
#define MENU_LINES_PER_SCREEN 5

// Задержка перед автоматическим возвратом в главное меню (миллисекунды)
#define MENU_AUTO_RETURN_TIMEOUT 60000  // 1 минута

// ======== НАСТРОЙКИ СКАНИРОВАНИЯ СЕТИ ========

// Максимальное количество сетей для отображения
#define MAX_WIFI_NETWORKS 20

// Интервал автоматического обновления данных сканирования (миллисекунды)
#define WIFI_SCAN_INTERVAL 30000  // 30 секунд

// ======== НАСТРОЙКИ СЕТЕВЫХ ИНСТРУМЕНТОВ ========

// Максимальное количество пингов за один запрос
#define MAX_PING_COUNT 20

// Таймаут пинга (миллисекунды)
#define PING_TIMEOUT 1000

// Максимальное количество IP-адресов для сканирования за один запрос
#define MAX_IP_SCAN_RANGE 254

// Максимальное количество заблокированных IP-адресов
#define MAX_BLOCKED_IPS 20

#endif // CONFIG_H